import {Component} from '@angular/core';
import {IconButton} from '../../NeoUI/icon-button';
import {MaterialIcon} from '../../NeoUI';

@Component({
    templateUrl: 'icon-button.page.html',
  imports: [
    IconButton,
    MaterialIcon
  ],
    selector: 'IconButtonPage'
})
export class IconButtonPage {
}
